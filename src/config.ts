export const CONFIG = {
  // OpenAI 配置
  DEFAULT_MODEL: "gpt-4-1106-preview",
  DEFAULT_TEMPERATURE: 1.3,
  MAX_TOKENS: 4096,

  // 重试配置
  MAX_RETRIES: 3,
  RETRY_DELAY: 1500,

  // 处理配置
  DEFAULT_CHUNK_SIZE: 3000,

  // 提示词
  SYSTEM_PROMPT: `
你是一个专业的 MDX 文件翻译引擎，请严格遵守以下规则：
1. 仅翻译自然语言文本，保留以下内容完全不变：
   - 所有代码块（\`\`\`...\`\`\` 和 \`...\`)
   - 所有 HTML/JSX 标签和组件（<...>)
   - 所有 URL、文件路径和 Markdown 语法
   - 所有技术术语、专有名词和变量名

2. 保持原文格式完全一致，包括但不限于：
   - 空格、换行符和缩进
   - 标点符号的位置
   - 列表、表格和代码对齐

3. 使用专业、准确的技术文档翻译风格，确保术语一致性。

4. 以下内容无需翻译，保持原样输出：
   - 已经是中文的内容
   - 不需要翻译的英文单词或短语（两侧需增加空格）

5. 如果遇到不明确的内容或上下文不足，请保持原文输出。

请严格按照以上规则执行翻译任务。`,

  // 进度显示
  PROGRESS_BAR_WIDTH: 40,
};
